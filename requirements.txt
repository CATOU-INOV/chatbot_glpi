# --- API ---
fastapi
uvicorn[standard]
pydantic-settings

# --- Ingestion Snowflake -> Qdrant ---
snowflake-connector-python
cryptography
beautifulsoup4
tqdm

# --- RAG (embeddings + base vectorielle) ---
qdrant-client
sentence-transformers
fastembed  # embeddings sparse BM25 pour la recherche hybride (dense + sparse)

# --- LLM local (Ollama, appelé en HTTP) ---
requests

# --- Observabilité ---
langfuse

# --- Config ---
python-dotenv
